version: 2.1

jobs:
  deploy:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - run:
          name: dependencies
          command: ./build_tools/circle/install.sh
      - run:
          name: test
          command: ./build_tools/circle/build.sh
      - run:
          name: deployment
          command: ./build_tools/circle/deploy.sh
      - store_artifacts:
          # Open the doc to the API
          path: ~/site

workflows:
  version: 2
  build-and-deploy:
      jobs:
      - deploy
